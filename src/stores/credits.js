// Pinia store for credits (stub)
import { defineStore } from 'pinia'
export const useCreditsStore = defineStore('credits', {
  state: () => ({
    creditBalance: 0,
  }),
  actions: {
    // TODO: Implement credit fetching
  },
})
